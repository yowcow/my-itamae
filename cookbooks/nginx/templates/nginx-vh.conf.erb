server {
    listen <%= @listen %>;
    listen [::]:<%= @listen %>;
    server_name <%= @server_name %>;

    access_log /var/log/nginx/<%= @server_name %>.access.log;
    error_log /var/log/nginx/<%= @server_name %>.error.log;

    location / {
        root /srv/<%= @server_name %>/current/public;
    }
}
